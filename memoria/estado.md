# Estado del proyecto — DoonFlow

> Última actualización: 2026-07-21

Este archivo es la "foto" actual del proyecto: qué existe, qué está en progreso y qué falta. Cada agente debe actualizarlo al terminar una tarea relevante (no para cambios triviales).

---

## Fase actual

**Levantamiento de requerimientos y arquitectura inicial.** Aún no hay código de aplicación (`apps/`, `packages/`) — solo gobierno del proyecto (`CLAUDE.md`, agentes, memoria).

---

## Hecho

- [x] `CLAUDE.md` con visión, stack, convenciones, arquitectura hexagonal (backend), arquitectura por capas (frontend), sistema de diseño (plantilla `nextjs-admin-dashboard-main`) y objetivo responsive tablet/desktop.
- [x] 11 subagentes definidos en `.claude/agents/` (6 técnicos: arquitecto, frontend, backend, basededatos, produccion, qa — 5 de dominio: fincas, trazabilidad, inventario, analisis, auth).
- [x] Convención de procedimientos almacenados (SP) y carpeta `packages/db/produccion/` para historial de cambios de BD.
- [x] Carpeta `memoria/` (este árbol) para registrar estado, decisiones, errores conocidos y solicitudes de integración entre módulos.
- [x] `memoria/cambios_db/` — bitácora de `agente-basededatos` para todo cambio de esquema (separada de `packages/db/produccion/`, que es solo lo validado para producción).
- [x] Scaffolding real del monorepo: raíz (`package.json`, `pnpm-workspace.yaml`, `turbo.json`, `tsconfig.base.json`), `packages/config`, `packages/shared`, `packages/ui` (Boton/GrupoInput/Casilla), `packages/db` (ver abajo), `apps/api`, `apps/web`.
- [x] `packages/db/prisma/schema.prisma`: `Empresa`, `Rol`, `Permiso`, `RolPermiso`, `Usuario` (multiempresa, enlazado a Supabase Auth vía `authUserId`). Seed con los 5 roles del sistema + empresa/usuario demo. RLS en `packages/db/sql/rls/auth_rls.sql` (sin aplicar — falta BD real).
- [x] `apps/api` módulo `auth` completo en arquitectura hexagonal (domain/application/infrastructure): caso de uso `IniciarSesionUseCase`, adaptador Supabase, repositorio Prisma, controller/router en `/auth/login`. Middleware `autenticacion`/`autorizacion` (guard de roles) en `apps/api/src/middleware/`.
- [x] `apps/web` página de login con arquitectura por capas (`_servicios`, `_hooks`, `_components`), estilo de la plantilla (dos columnas, panel de marca desde `md:`). Middleware JWT verifica cookie `doonflow_token`.
- [x] **Login end-to-end funcionando** — PostgreSQL 13 local (puerto 5433), sin Supabase. `LocalAuthAdapter` (bcrypt + JWT propio). Migración `init_auth` aplicada. Seed con 5 roles + empresa demo + usuario `admin@doonflow.demo` / `Admin2026!`.
- [x] Route Handler `POST /api/auth/login` (Next.js) actúa de proxy → API Express → setea cookie `httpOnly`.
- [x] `apps/api` corriendo en `http://localhost:4000` modo LOCAL. `apps/web` en `http://localhost:3000`.
- [x] **Sprint 003 — Sucursales — IMPLEMENTADO (2026-07-17)**: primer sprint de la lista de 003-014 en pasar de solo-specs a código real. `cliente.movil`, módulo `sucursales` completo (hexagonal, montado en `/sucursales`), extensión de `auth` con Usuario↔Sucursal (solo dominio/repositorio, sin router — lo expone 004 según lo acordado), proxy Next.js, sección "Sucursales" en `/mantenimientos/clientes/[id]/editar`, página de histórico en `/mantenimientos/clientes/[id]/sucursales/[sucursalId]`. Migración `20260717000000_add_sucursales` aplicada en dev (psql + registro manual en `_prisma_migrations`, entorno no interactivo). Seed de 2 clientes demo + 3 sucursales (idempotente). 55 tests backend + 16 tests frontend en verde. Verificado end-to-end vía curl (API directa y a través del proxy Next.js con cookie de sesión real): crear/listar/desactivar sucursal, aislamiento multiempresa (404 en cliente de otra empresa), 401 sin token. **No verificado interactivamente en navegador** (sin herramienta de automatización de browser disponible en esta sesión) — pendiente que el usuario confirme el flujo visual (modal de sucursal, tabla, historial) la próxima vez que abra la app.
- [x] **Sprint 004 — Usuarios, roles y alcance de acceso — IMPLEMENTADO (2026-07-17/18)**: `usuario.clienteId` + 2 roles nuevos (`administrador_cliente`, `usuario_sucursal`); CRUD completo de usuarios en `/usuarios` (solo `administrador`, HU-1); `GET /auth/me` con alcance resuelto (`TOTAL`/`CLIENTE`/`SUCURSAL`); middleware `resolverAlcance` filtrando `GET /clientes` y `GET /sucursales` según el alcance del usuario autenticado (defensa en profundidad); frontend: `/mantenimientos/usuarios` (lista/crear/editar), `/mi-empresa` (redirige a detalle de cliente propio en modo `soloLectura=1`), `/mi-sucursal` (histórico acotado, con selector si hay más de una sucursal), guards por rol en `middleware.ts` (decodifica el JWT sin verificar firma — solo UX, la verificación real ya ocurre en cada request del backend). **Reconciliación con 003**: `requiereSucursalAsignada`/`tieneAsignacionValida`/`GestionarAccesoSucursalUseCase` de 003 (que asumían "todo rol no-admin requiere sucursal", escrito antes de que existieran `administrador_cliente`/`usuario_sucursal`) quedaron **reemplazados** por el modelo más preciso de 004 (`requiereClienteId`/`requiereSucursalId`, solo aplican a esos 2 roles nuevos) — se eliminó el usecase y el test de 003 por quedar redundantes con `GestionarUsuarioUseCase`. Ver [[004-usuarios-roles-alcance]]. T-172 (bloquear escritura de inspecciones para roles externos) **no implementado**: no existe todavía ningún endpoint de ejecución/certificación de inspecciones (RF-08, sprint 015 pendiente) — no hay nada que bloquear aún; retomar cuando 015 exista. 77 tests backend + 98 tests frontend en verde (con la única excepción preexistente de `strip-resumen-plantilla.test.tsx`, sprint 001, no tocado). **Bug real encontrado y corregido durante la verificación E2E**: `cliente.prisma-repository.ts` y `sucursal.prisma-repository.ts` construían el filtro de `obtenerPorId` como `{ id, empresaId, ...whereAlcance(alcance) }` — cuando `whereAlcance` retornaba una clave `id` (caso alcance `CLIENTE`/`SUCURSAL`), el spread **pisaba silenciosamente** el `id` solicitado en la URL y devolvía un recurso distinto al pedido (200 con datos equivocados) en vez de 404. Ningún test unitario lo detectó porque todos mockean el repositorio — solo aparece con Prisma real. Corregido evitando la colisión de claves; verificado con curl que ahora sí retorna 404 correcto. **Lección para 007+**: cualquier filtro construido por spread de objetos debe revisarse por colisión de claves cuando se combina con un `id` ya presente en el `where` — no es detectable con mocks, requiere probar contra BD real. Verificado end-to-end vía curl con los 3 roles reales (admin/administrador_cliente/usuario_sucursal): login, `/auth/me`, filtrado de `/clientes` y `/sucursales`, creación de usuario por rol, validaciones 400/409, desactivar usuario + bloqueo de login, guards de `middleware.ts` (redirect fuera de rutas no permitidas), y las páginas del navegador (sin clics reales, solo carga/compilación — mismo límite que 003).
- [x] **Sprint 015 — ⭐ HU PRINCIPAL DE TODO EL DESARROLLO — Wizard de certificación — IMPLEMENTADO (2026-07-18/20, en dos sesiones)**: `memoria/sprint/015-wizard-certificacion/`. Cubre HU-1 completa: Paso 0 (Cliente → Sucursal → Período, usa la plantilla activa vigente automáticamente) → un paso por cada sección de nivel 0 de la Ficha BPM → paso final de Revisión (puntaje/porcentaje/clasificación por sección) → "Guardar y finalizar" (navega al listado). **Sin firma digital, sin PDF, sin código de verificación**, como estaba decidido. Backend: módulo `inspeccion` ampliado con `certificacion.entity.ts`/`evidencia.entity.ts` (dominio), `iniciar-certificacion`/`responder-certificacion` (casos de uso), `certificacion.prisma-repository.ts`, adaptadores dual Local/Supabase de evidencias (mismo patrón que `auth`), middleware `multer` de subida, endpoints montados en `/inspeccion/certificaciones/**`. Frontend: `certificacion.servicio.ts`, 6 hooks (`usar-iniciar-certificacion`, `usar-responder-certificacion`, `usar-wizard-certificacion` ⭐, `usar-revision-certificacion`, `usar-certificaciones`, helper `utilidades-arbol`), componentes dedicados nuevos (`wizard-certificacion`, `indicador-progreso-wizard`, `seccion-wizard`, `pregunta-wizard`, `respuesta-widget`, `tabla-certificaciones`), páginas `certificaciones/page.tsx` (listado, no estaba en el plan original pero es necesario como destino del flujo), `nueva/`, `[id]/responder/`, `[id]/revision/`, ítem "Certificaciones" en el sidebar. Eliminado el placeholder obsoleto `inspecciones/[id]/ejecutar/page.tsx`. **Decisión de implementación (2026-07-18)**: en vez de retrofitear `tabla-ficha.tsx`/`fila-seccion.tsx`/`fila-pregunta.tsx` del editor de 001 con un prop `modo="responder"` (plan original de `task.md`), se construyeron componentes nuevos y dedicados para el wizard — menor riesgo de regresión sobre un componente de edición ya en producción. Ver detalle de la desviación en `impl.md`/`task.md` del sprint. **Bug de infraestructura encontrado y corregido**: el proxy Next.js genérico de `/api/inspeccion/[...path]` forzaba `Content-Type: application/json` y leía el body como texto en toda petición — rompía la subida de evidencias (`multipart/form-data`). Corregido para reenviar el cuerpo crudo (`blob()`) respetando el `Content-Type` original; verificado con curl (subida real de archivo a través del proxy, HTTP 201). 93 tests backend + 116 tests frontend en verde (con la única excepción preexistente de `strip-resumen-plantilla.test.tsx`, sprint 001). Verificado end-to-end vía curl contra la API real y a través del proxy con sesión real: iniciar certificación, guardar respuestas por sección (cálculo de puntaje SI_NO/manual correcto), subir evidencia, resumen con puntaje/porcentaje/clasificación por sección, 401 sin token, 400 validación, 422 archivo no permitido, y las 4 páginas del wizard cargando sin error de servidor. **No verificado con clics reales en un navegador** (sin herramienta de automatización de browser en esta sesión, mismo límite que 003/004). **Gap preexistente detectado (no introducido por este sprint)**: `pnpm lint` no está configurado en el proyecto (`apps/api` no tiene script `lint`; `apps/web`/`next lint` nunca se configuró) — pendiente para cuando se decida abordarlo, no bloqueante para este sprint. Ver checklist completo en `memoria/sprint/015-wizard-certificacion/checklist.md`.
- [x] **Sprint 007 — Gobernanza: permisos por rol y aprobación de plantillas — IMPLEMENTADO (2026-07-21, en dos sesiones)**: `memoria/sprint/007-gobernanza-permisos-aprobacion/`. Cubre las 2 HU: matriz Rol×Permiso editable en `/mantenimientos/roles` (rol `administrador` con todos los permisos, no editable) y flujo de aprobación de plantillas BPM (`BORRADOR → EN_REVISION → APROBADA/RECHAZADA`, con reversión automática a `BORRADOR` al editar una plantilla ya resuelta) antes de poder certificar con ellas — `puedeIniciarInspeccion()` ahora exige `estadoAprobacion === "APROBADA"`. Backend: módulo nuevo `apps/api/src/modules/permisos/` (hexagonal completo) + middleware `requierePermiso(codigo)` en `apps/api/src/middleware/permiso.middleware.ts` (con caché de proceso invalidado tras `PUT /permisos/roles/:rolId`) + extensión del módulo `inspeccion` (4 endpoints de aprobación, `InspeccionAuditoria` poblada por primera vez desde 001). Frontend: `/mantenimientos/roles` (matriz), `/inspecciones/aprobaciones` (cola de aprobación con `DialogoRechazoPlantilla`), badge `BadgeEstadoAprobacion` + botón "Enviar a revisión" en el editor de plantilla y en `tabla-plantillas.tsx`. **Desviaciones documentadas en `impl.md`**: (1) el caché de `requierePermiso()` indexa por `rolNombre`, no `rolId`, porque `req.usuario` (módulo `auth`) nunca trae `rolId`; (2) los 4 endpoints de aprobación no necesitaron Route Handler propio — ya los cubre el catch-all `apps/web/src/app/api/inspeccion/[...path]/route.ts` existente desde 015. **Bugs reales encontrados y corregidos**: `GestionarPlantillaUseCase.actualizar()` no devolvía los campos limpiados al revertir a `BORRADOR` (BD correcta, respuesta HTTP con datos viejos); el seed de la plantilla demo no forzaba `estadoAprobacion: "APROBADA"`, rompiendo el wizard de 015 tras agregar el nuevo gate (**recordar correr `pnpm --filter db seed` de nuevo si se resetea la BD**). 126 tests backend + 137 tests frontend en verde (111→137, +26 tests nuevos de este sprint; la única excepción preexistente sigue siendo `strip-resumen-plantilla.test.tsx` de sprint 001, no tocado). Verificado end-to-end vía curl contra la API real y a través del proxy con sesión real: ciclo completo iniciar→enviar-revisión→aprobar/rechazar→revertir a borrador, 401/403/422/400 en los casos de error, permiso denegado a un `administrador_cliente` sin `plantillas.aprobar`, endpoints de datos reales (`/api/permisos/matriz`, `/api/inspeccion/plantillas/pendientes-aprobacion`) respondiendo con datos consistentes, y las 2 páginas nuevas cargando sin error de servidor. **No verificado con clics reales en un navegador** (mismo límite que 003/004/015). Ver checklist completo en `memoria/sprint/007-gobernanza-permisos-aprobacion/checklist.md`.
- [x] **Sprint 010 — Seguridad, privacidad y continuidad — IMPLEMENTADO (2026-07-21, misma sesión que cerró 007)**: `memoria/sprint/010-seguridad-privacidad-continuidad/`. RNF transversales, mayormente backend/infraestructura. Cubre: **auditoría transversal** (`RegistroAuditoria`, append-only — módulo nuevo `apps/api/src/modules/auditoria/`, integrado en `LOGIN`/`LOGIN_FALLIDO`, `PERMISO_MODIFICADO`, `USUARIO_DESACTIVADO`; `CLIENTE_DESACTIVADO`/`SUCURSAL_DESACTIVADO` quedaron pendientes de integrar, mismo patrón ya establecido); **política de retención** (`PoliticaRetencion` por tipo de dato, módulo `apps/api/src/modules/retencion/`, SP `sp_retencion_purgar_datos` + job diario con `node-cron`); **aviso de privacidad** (`AvisoPrivacidad`, módulo `apps/api/src/modules/privacidad/`, montado bajo `/privacidad/:entidadTipo/:entidadId` en vez de anidado bajo `/clientes`/`/sucursales` como decía el spec original); **sesión y contraseña** (distinción `sesion_expirada`/`token_invalido` en `autenticacion.middleware.ts`, redirect real a `/auth/login?motivo=sesion_expirada`, `validarPoliticaPassword()` + endpoint `POST /usuarios/:id/cambiar-password`); **paginación server-side real** en Clientes y Certificaciones (componente `Paginador` nuevo en `packages/ui`) — Sucursales usa el endpoint paginado sin exponer la UI de `Paginador` (decisión de alcance, lista naturalmente acotada por cliente); **política de backup** documentada en `packages/db/produccion/politica-backup.md` (RPO/RTO declarados, estado explícitamente provisional sin ambiente de producción todavía). **Bug real de seguridad encontrado y corregido en la misma sesión**: el módulo `privacidad` inicialmente no validaba que `entidadId` perteneciera a la empresa del usuario autenticado — corregido inyectando `ClienteRepositoryPort`/`SucursalRepositoryPort` en `GestionarAvisoPrivacidadUseCase`, verificado por curl (404 ante entidad de otra empresa). **Desviaciones documentadas en `impl.md`** (7 en total): `JWT_EXPIRES_IN` reutilizado en vez de una variable nueva; rutas reales `/mantenimientos/auditoria`/`/mantenimientos/retencion` (no `/configuracion/*`, esa sección no existe en el sidebar real); `sp_retencion_purgar_datos` tipado `TEXT` no `UUID` (todos los IDs del schema son `TEXT`); `PDF_CERTIFICACION` sin efecto en el SP (columna `pdf_url` no existe, 005 sigue pausado); `DATO_PERSONAL_CONTACTO` nunca hace `DELETE` real (solo anonimiza, y solo sobre registros inactivos) por seguridad. 140 tests backend + 153 tests frontend en verde (126→140 backend, 137→153 frontend; misma única excepción preexistente de sprint 001). Verificado end-to-end vía curl con sesión real: ciclo completo de cambio de contraseña (actual incorrecta/nueva débil/válida/revertida), paginación de clientes/certificaciones con `meta.total` correcto, matriz de auditoría reflejando eventos `LOGIN` reales, aviso de privacidad con aislamiento multiempresa, 403 real con un rol sin permiso de auditor. **No verificado con clics reales en un navegador** (mismo límite que 003/004/007/015) ni con datos de retención realmente vencidos (requeriría sembrar filas con antigüedad artificial). Ver checklist completo con todas las salvedades honestas en `memoria/sprint/010-seguridad-privacidad-continuidad/checklist.md`.
- [x] **Sprint 008 — Reportes y Analytics — IMPLEMENTADO (2026-07-21, misma sesión que cerró 007 y 010)**: `memoria/sprint/008-reportes-analytics/`. **Alcance reducido por decisión explícita del usuario** (preguntado directamente al detectar el bloqueo): el spec original dependía de campos/tablas que no existen (`Inspeccion.firmadoEn`/`fechaVencimiento` de 005, pausado; `Hallazgo`/`PlanCumplimiento`/`AccionCorrectiva` de 013, bloqueado) — los reportes de este sprint usan solo los datos reales del wizard de 015 (`puntajeObtenido`/`porcentajeCumplimiento`/`clasificacion`), sin hallazgos/plan de cumplimiento/vigencia. Cubre: módulo nuevo `apps/api/src/modules/reportes/` (hexagonal completo: `GenerarReporteUseCase`, `ListarHistorialReportesUseCase`, puertos `GeneradorExcelPort`/`GeneradorPdfPort`/`ReporteStoragePort`), reporte consolidado por cliente y comparativo entre sucursales, exportables a Excel (`exceljs`) y PDF (`pdfkit`, decisión propia de este sprint — ver `memoria/decisiones.md`), historial de reportes (`ReporteGenerado`, nunca se borra) con `resumenFiltros`/`generadoPorNombre` resueltos server-side. Frontend: `/analytics` (página nueva, no existía panel ejecutivo de 006/014 que actualizar), `/analytics/reportes` (historial), `/analytics/reportes/nuevo` (formulario controlado + vista previa antes de exportar), componente transversal `TablaComparativa` nuevo en `packages/ui`. **Bug real de control de acceso encontrado y corregido en la misma sesión**: `ListarHistorialReportesUseCase.listar()` no bloqueaba a `usuario_sucursal` (podía ver el historial completo de la empresa) — corregido, verificado por curl con el usuario demo `lucia@dist.com`. **Inconsistencia preexistente corregida de paso**: `packages/ui` declaraba `@types/react ^19` mientras el resto del monorepo usa `^18`, causando un choque de tipos `ReactNode` — alineado a `^18.3.0`. 157 tests backend + 165 tests frontend en verde (140→157 backend, 153→165 frontend; misma única excepción preexistente de sprint 001). Verificado end-to-end vía curl contra la API real y a través del proxy con sesión real: generación real de reportes Excel y PDF (archivos binarios validados, no solo HTTP 201), historial con filtros resueltos, bloqueo de acceso por rol (`administrador_cliente` con cliente ajeno → 403, `usuario_sucursal` → 403). **No verificado con clics reales en un navegador**. Ver todas las desviaciones (9 en total) y salvedades en `memoria/sprint/008-reportes-analytics/impl.md` y `checklist.md`.
- [x] **Sprint 009 — Integraciones y datos masivos — IMPLEMENTADO (2026-07-21, misma sesión que cerró 007/010/008), alcance reducido: HU-1/HU-2 completas, HU-3 pospuesta**: `memoria/sprint/009-integraciones-datos-masivos/`. **HU-3 (API keys + endpoint público de verificación) pospuesta por decisión explícita del usuario** (preguntado al detectar el bloqueo): depende del portal `/verificar/[codigo]` de 006 (nunca implementado, sin código) y de `Inspeccion.codigoVerificacion`/`fechaVencimiento` (campos de 005, pausado). Cubre completas HU-1 e HU-2: módulo nuevo `apps/api/src/modules/integraciones/` (hexagonal, solo el subconjunto de importación: `ImportacionLote`, `ImportarClientesUseCase`/`ImportarSucursalesUseCase` con modo previsualización dry-run real, `plantilla-excel.ts` con `xlsx` para generar/parsear `.xlsx`), extensión mínima permitida de `clientes` (`buscarPorIdentificacion`), subida de archivo vía `multer`, proxy Next.js catch-all con passthrough binario (necesario para descargar plantillas y detalle de errores). Frontend: componente genérico `ModalImportarExcel` + hook `usarImportacionExcel` (máquina de estados `INACTIVO→...→COMPLETADO`), integrado en `/mantenimientos/clientes` (botón junto a "+ Agregar cliente") y en la sección "Sucursales" de la edición de cliente. **No se creó**: modelo/tabla `ApiKey`, ningún caso de uso ni endpoint de API keys, el endpoint público `/api/v1/certificaciones/verificar/:codigo`, los middlewares `autenticacion-api-key`/`limitador-tasa`, la pantalla `/configuracion/integraciones` ni el ítem de sidebar correspondiente — ver las 12 desviaciones documentadas en `impl.md`. 167 tests backend + 174 tests frontend en verde (157→167 backend, 165→174 frontend; misma única excepción preexistente de sprint 001). Verificado end-to-end vía curl dos veces (directo contra `apps/api` puerto 4000 y a través del proxy real de Next.js puerto 3000): descarga de plantilla (magic bytes `PK` verificados), previsualizar/confirmar para clientes y sucursales (incluidos casos de error: correo inválido, identificación duplicada, cliente inexistente), descarga de detalle de errores, botón "Importar desde Excel" renderizando en la página real de clientes. **No verificado con clics reales en un navegador**. Ver `memoria/sprint/009-integraciones-datos-masivos/impl.md` y `checklist.md` para el detalle completo de qué quedó pospuesto y por qué.
- [x] **Sprint 012 — Captura offline en campo — IMPLEMENTADO (2026-07-22), con T-490/T-501 (firma) bloqueadas por 005 pausado en su momento**: `memoria/sprint/012-captura-offline-campo/`. Amplía el wizard de certificación de 015 con captura offline real: `apps/web/src/lib/offline/` (IndexedDB nativo sin librería `idb` — `db-offline.ts`, `respuestas-offline.store.ts`, `evidencias-offline.store.ts` con compresión de imágenes vía `<canvas>`, `cola-sincronizacion.ts` con backoff exponencial y tolerancia a fallo parcial, `estado-conexion.ts`), hook `usarCapturaOffline` (estado derivado de sincronización, 5 valores incluyendo `null` = "nada que comunicar"), `BannerEstadoConexion`/`BadgeCapturaOffline`. Backend: `Inspeccion.capturaOffline`/`sincronizadoEn` + 3 endpoints nuevos en el módulo `inspeccion` (`POST .../sincronizacion`, `POST .../sincronizacion/evidencias`, `GET .../sincronizacion/estado`) reutilizando el catch-all de proxy ya existente desde 015 (**sin Route Handlers nuevos**), `SincronizarCapturaOfflineUseCase` con resolución de conflictos por última escritura y auditoría vía el patrón ya establecido en 010 (`RegistradorEventoAuditoria`). El puntaje siempre se recalcula server-side, nunca se confía el del cliente. **T-490/T-501 (bloquear firma sin sincronizar) quedaron implementadas al retomarse 005** (ver más abajo) — `puedeFirmarse()`/`SincronizacionPendienteError`, ya listos en el dominio desde este sprint, ahora los usa `FirmarCertificacionUseCase`. **Limitación real encontrada y documentada (no corregida, ver `memoria/decisiones.md`)**: un reintento legítimo del mismo cliente con el mismo payload se clasifica como falso-positivo de conflicto porque la comparación usa `InspeccionDetalle.actualizadoEn` (hora del servidor) en vez del último `capturadoEnCliente` aplicado — sin pérdida de datos, pero generaría auditoría de conflicto innecesaria; corregirlo de raíz requiere una columna nueva, fuera de alcance de este sprint. 174 tests backend (+7) / 195 tests frontend (+21) en verde, misma única excepción preexistente de sprint 001. Verificado end-to-end vía curl directo contra `apps/api` y a través del proxy real de Next.js: sincronización de lote (idempotencia + el falso-positivo documentado), evidencia resolviendo `nodoId→detalleId`, 409/413/401, y las páginas `responder`/`revision`/listado cargando sin error de servidor. **No verificado con clics reales en un navegador** ni con DevTools → Offline real. Ver `memoria/sprint/012-captura-offline-campo/impl.md` y `checklist.md` para el detalle completo de las 17 desviaciones documentadas.
- [x] **Sprint 005 — Certificación, HU-2 (firma digital/PDF/código de verificación) — RETOMADO E IMPLEMENTADO (2026-07-21)**: `memoria/sprint/005-certificacion-plan-cumplimiento/`. El usuario decidió retomarlo en vez de continuar con el sprint 014 sugerido. Cubre HU-2 completa sobre una certificación ya completada por el wizard de 015: `POST /inspeccion/certificaciones/:id/firmar` (bloquea si ya no está `EN_PROGRESO` o si hay pendientes de sincronizar sin resolver, delega en `sp_inspeccion_firmar` — nuevo SP que recalcula puntaje/porcentaje/clasificación de forma atómica y persiste `estado=FIRMADA`/`firmadoPorId`/`firmadoEn`/`codigoVerificacion`/`fechaVencimiento`/`resultadoFinal='APROBADA'`), genera un PDF real (pdfkit, no Puppeteer — decisión de esta sesión, ver `impl.md`) y lo sube a almacenamiento dual local/Supabase en buckets propios (`certificaciones-pdf`/`certificaciones`), `GET /inspeccion/certificaciones/:id/pdf` (retorna `{ url }`, mismo patrón que la descarga de reportes de 008). Frontend: botón "Firmar y certificar" que **convive** con "Guardar y finalizar" en `/certificaciones/[id]/revision`, vista post-firma con código de verificación/vigencia/descarga de PDF. `codigoVerificacion` único en todo el sistema (índice único), `Inspeccion` FIRMADA bloquea edición de respuestas también a nivel de API (no solo UI). 191 tests backend (+6) / 199 tests frontend (+4) en verde, misma única excepción preexistente de sprint 001. Verificado end-to-end vía curl (directo y a través del proxy Next.js con sesión real): firma de 3 certificaciones demo reales con códigos de verificación distintos, PDF real descargado (`%PDF` confirmado), doble firma → 409, pendientes de sincronizar → 409, edición post-firma → 409, página de revisión cargando sin error de servidor. **No verificado con clics reales en un navegador.** Ver `memoria/sprint/005-certificacion-plan-cumplimiento/impl.md` y `checklist.md` para las 5 desviaciones documentadas. **Efecto en cascada**: 013-hallazgos-plan-cumplimiento ya no está bloqueado por la ausencia de 005 (aunque sigue sin implementarse); 006-HU1/HU3 (vigencia/portal) y 009-HU3 (API keys) ahora tienen los campos que necesitaban (`codigoVerificacion`/`fechaVencimiento`) disponibles para retomarse cuando se decida.
- [x] **Sprint 013 — Hallazgos y plan de cumplimiento — IMPLEMENTADO (2026-07-22)**: `memoria/sprint/013-hallazgos-plan-cumplimiento/`. El usuario lo eligió en vez de 014 (parcial) tras cerrarse 005. Cubre las 3 HU completas: hallazgos (automáticos por respuesta incumplida + manuales), plan de cumplimiento con acciones correctivas (avance del responsable, verificación del auditor, cierre/reapertura), y el cálculo real de `resultadoFinal` por severidad (antes fijo en `APROBADA` desde 005). BD: 5 tablas nuevas (`hallazgo`, `hallazgo_evidencia`, `plan_cumplimiento`, `accion_correctiva`, `accion_correctiva_evidencia`) + `sp_inspeccion_firmar` reemplazado (bloquea la firma por hallazgo `CRITICA` sin acción `CUMPLIDO`, calcula `resultado_final` real) + SP nuevo `sp_plan_cumplimiento_indicadores`. Backend: mismo módulo `inspeccion`, 3 entidades de dominio + 3 puertos + 4 casos de uso + 3 repositorios Prisma + 3 controllers; `firmar-certificacion.usecase.ts` (005) ampliado para traducir la excepción del SP y para incluir los hallazgos en el PDF. Frontend: 2 servicios, 4 hooks nuevos + `usar-revision-certificacion` ampliado, 4 páginas nuevas (`hallazgos`/`plan`/`seguimiento`/`verificacion`) + Pantalla 3 ampliada, 8 componentes nuevos, sidebar con "Mis acciones"/"Verificación" (esta última con guard de rol en `middleware.ts`). 231 tests backend (+40) / 220-de-226 tests frontend (+21 nuevos; misma única excepción preexistente de sprint 001) en verde. **Verificado end-to-end de punta a punta vía curl** (directo y a través del proxy Next.js con sesión real): flujo completo respuestas incumplidas→hallazgos automáticos (idempotente)→bloqueo de firma por crítico→plan→acciones (avance/evidencia/revisión/verificación CUMPLIDO y NO_CUMPLIDO)→firma exitosa con `APROBADA_CON_OBSERVACIONES`→PDF real con hallazgos→cierre/reapertura del plan; 401/403/404 reales (incluida la regla de no-autoverificación); guard de `middleware.ts` verificado con clic real. **No verificado con clics reales en un navegador.** **2 desviaciones no triviales**: no existe el seed `certificaciones-demo.ts` que 005/013 daban por hecho (nunca se creó, ni en 005) — la verificación usa una certificación real vía API; no se agregaron policies RLS nuevas (mismo patrón sin RLS real desde 007). Ver `memoria/sprint/013-hallazgos-plan-cumplimiento/impl.md` para el detalle completo de las 8 desviaciones y `checklist.md` para el resultado ítem por ítem. **Gap preexistente encontrado (no de este sprint, documentado para `agente-frontend`)**: `packages/ui/src/breadcrumb.tsx` importa `next/link` sin declarar `next` como dependencia del paquete — bloquea `next build`/`next lint`, ya reportado como pendiente.

## En progreso

_Nada activo actualmente._

## Pendiente (no iniciado)

- [ ] **Orden sugerido de implementación (2026-07-16, solo para referencia futura — no se ha empezado a implementar nada)**:
  1. **003** (Sucursales) — prerrequisito de casi todo.
  2. **004** (Usuarios/roles/alcance).
  3. **015** (⭐ Wizard de certificación) — flujo principal, sin bloqueos tras la pausa de la firma.
  4. **007** (Gobernanza: permisos + aprobación de plantillas) — ✅ implementado, ver "Hecho".
  5. **010** (Seguridad/privacidad/continuidad) — ✅ implementado, ver "Hecho".
  6. **008** (Reportes y Analytics) — ✅ implementado (alcance reducido), ver "Hecho".
  7. **009** (Integraciones y datos masivos) — ✅ implementado (HU-1/HU-2, HU-3 pospuesta), ver "Hecho".
  8. **012** (Captura offline) — ✅ implementado (T-490/T-501, bloqueadas por 005 en su momento, ya implementadas al retomarse).
  9. **005** (Certificación HU-2, firma/PDF/código de verificación) — ✅ implementado (2026-07-21, retomado a pedido del usuario en vez de continuar con 014), ver "Hecho".
  10. **013** (Hallazgos y plan de cumplimiento) — ✅ implementado (2026-07-22, elegido por el usuario en vez de 014 parcial), ver "Hecho".
  11. **Siguiente en el orden sugerido**: confirmar con el usuario entre **014 parcial** (HU-4 panel ejecutivo, ya desbloqueado por 013; HU-5/HU-6 nunca bloqueadas), **011** (aceptación/apelaciones, ya no bloqueado por hallazgos — 013 implementado) y **006** (HU-1/2/3/7 vigencia/notificaciones/portal, ya no bloqueado por 013 tampoco). Ninguno de los tres tiene bloqueos pendientes a esta fecha.

- [x] **Sprint 003 — Sucursales por cliente y certificación por sucursal — IMPLEMENTADO (2026-07-17)**. Ver detalle arriba en "Hecho". Su parte de asignación Usuario↔Sucursal quedó recortada a solo dominio/repositorio (T-117–T-122) — la expone y muestra en pantalla [[004-usuarios-roles-alcance]] para no duplicar el router `/usuarios` (ver nota de alcance en su `task.md`).
- [x] **Sprint 004 — Usuarios, roles y alcance de acceso — IMPLEMENTADO (2026-07-17/18)**. Ver detalle arriba en "Hecho".
- [x] **Sprint 015 — ⭐ HU PRINCIPAL DE TODO EL DESARROLLO — Wizard de certificación — IMPLEMENTADO (2026-07-18/20)**. Ver detalle arriba en "Hecho".
- [x] **Sprint 007 — Gobernanza: permisos por rol y aprobación de plantillas — IMPLEMENTADO (2026-07-21)**. Ver detalle arriba en "Hecho".
- [x] **Sprint 010 — Seguridad, privacidad y continuidad — IMPLEMENTADO (2026-07-21)**. Ver detalle arriba en "Hecho".
- [x] **Sprint 008 — Reportes y Analytics — IMPLEMENTADO (2026-07-21, alcance reducido)**. Ver detalle arriba en "Hecho".
- [x] **Sprint 009 — Integraciones y datos masivos — IMPLEMENTADO (2026-07-21, HU-1/HU-2 completas, HU-3 pospuesta)**. Ver detalle arriba en "Hecho".
- [x] **Sprint 012 — Captura offline en campo — IMPLEMENTADO (2026-07-22, T-490/T-501 bloqueadas por 005 en su momento, ya implementadas)**. Ver detalle arriba en "Hecho".
- [x] **Sprint 005 — Certificación, HU-2 (firma/PDF/código de verificación) — RETOMADO E IMPLEMENTADO (2026-07-21)**. Ver detalle arriba en "Hecho".
- [x] **Sprint 013 — Hallazgos y plan de cumplimiento — IMPLEMENTADO (2026-07-22)**. Ver detalle arriba en "Hecho".
- [ ] **Sprint 006 — Vigencia, notificaciones y portal de verificación — ya no bloqueado**: HU-1 (vencimiento) y HU-3 (portal/QR) no dependían de 013. HU-2/HU-7 (notificaciones de acción/escalamiento) dependían de 013 (ahora implementado) — sprint completo desbloqueado. T-270–T-327 (40 tareas).
- [ ] **Sprint 014 — Panel ejecutivo, calendario y biblioteca — ya no bloqueado**: HU-4 (panel ejecutivo) dependía de 013 (ahora implementado); HU-5 (calendario) y HU-6 (biblioteca de hallazgos) nunca estuvieron bloqueadas. T-600–T-622 (23 tareas).
- [ ] **Sprint 011 — Aceptación y apelaciones de certificación — ya no bloqueado** (dependía de hallazgos/013, ahora implementado): Spec + `task.md`/`checklist.md`/`impl.md` completos (T-450–T-481, 2026-07-16) — revisar contra el `impl.md` real de 013 antes de implementar (nombres de módulo/campos), mismo criterio ya aplicado repetidamente en este proyecto.
- [ ] **Sprint 009 — HU-3 (API keys + endpoint público de verificación) — pospuesta, parcialmente desbloqueada**: dependía de `Inspeccion.codigoVerificacion`/`fechaVencimiento` (ahora existen desde 005) y del portal `/verificar/[codigo]` de 006 (sigue sin implementarse). Podría retomarse si se implementa el portal de 006 primero.
- [ ] **Decisión de negocio pendiente (2026-07-16, sin resolver)**: si DoonFlow cobra por certificación (facturación/pagos) y si habrá solicitud de certificación self-service por parte del cliente. Se preguntó explícitamente y el usuario respondió "no estoy seguro / decidir después" — **no se creó HU de facturación** para no asumir alcance comercial no confirmado. Retomar esta pregunta antes de planear sprints 013+.
- [ ] **Todos los sprints 003–012 tienen ahora spec + task + checklist + impl completos (2026-07-16), pero SIGUEN SIN CÓDIGO** — son documentos de planificación generados por agentes en paralelo a partir de las specs, sin implementar todavía. La numeración de tareas usa bloques reservados por sprint (003: T-100s, 004: T-150s, 005: T-200s, 006: T-270s, 007: T-330s, 008: T-360s, 009: T-390s, 010: T-420s, 011: T-450s, 012: T-480s/T-510s) — antes de sumar un sprint 013 nuevo, reservar su propio bloque de 30-70 IDs para no repetir la colisión que se corrigió entre 011/012.
- [x] **Auditoría cruzada 001-012 (2026-07-16)**: verificados sin más colisiones de T-ID, mounts de router Express y sidebar.tsx. Se corrigió un segundo problema real: **006, 010, 011 y 012 asumieron de forma independiente (y entre sí inconsistente: `modules/certificacion/` singular en unos, `modules/certificaciones/` plural en otro) que el motor de certificación vivía en un módulo backend nuevo**, cuando en realidad `005-certificacion-plan-cumplimiento/impl.md` lo puso **dentro de `apps/api/src/modules/inspeccion/`** (el mismo módulo del editor de plantillas de 001, montado bajo el prefijo `/inspeccion`). Se corrigieron rutas de archivo, nombres de caso de uso (`gestionar-hallazgos.usecase.ts`, `gestionar-accion-correctiva.usecase.ts`, `pdf-certificacion.adapter.ts`, `inspeccion.router.ts`) y prefijos de endpoint (`/inspeccion/certificaciones/:id/...`) en los 4 sprints afectados. También se corrigió que 010 asumía un módulo `modules/usuarios/` para la política de contraseña, cuando 004 puso el CRUD de usuarios dentro de `modules/auth/`. Antes de implementar cualquiera de estos sprints, verificar contra el `impl.md` del sprint del que depende en vez de asumir nombres de módulo.
- [x] **División de sprints grandes 005 y 006 (2026-07-16, a pedido del usuario)**: 006 (60 tareas) → **006** (Parte A, notificaciones+portal, T-270–T-327) + **014-panel-calendario-biblioteca** (Parte B, T-600–T-622). 005 (70 tareas) → **013-hallazgos-plan-cumplimiento** (T-520–T-570). Tras dividir, se corrigieron referencias cruzadas que quedaron apuntando al sprint equivocado: `PLAN_CERRADO` en 010 (era 005, es 013), referencias a `Hallazgo`/regla de severidad 1.1 en 011 (eran 005, son 013), el panel ejecutivo citado en 008/013 (era 006, es 014).
- [x] **Extracción del wizard a sprint 015 + pausa de la firma digital (2026-07-16, a pedido explícito del usuario: "mejor en una HU aparte" → "no en la misma 005" → "la firma digital no implementarla" → "solo el formulario")**: el wizard de captura de respuestas (antes HU-1 de 005) se movió a **[[015-wizard-certificacion]]** (carpeta nueva, T-200–T-244 — el rango que 005 tenía originalmente). **005 quedó pausado**, recortado a solo HU-2 (firma/PDF/código de verificación) y renumerado a T-650–T-669 para no colisionar con 015. Esto generó un **bloqueo en cascada** ya documentado en `estado.md`: 013 (depende de 005) → bloqueado por completo; 006 y 014 → bloqueados parcialmente (solo las HU que necesitan `fechaVencimiento`/`codigoVerificacion` o `Hallazgo`/`AccionCorrectiva`); 011 → bloqueado por completo; 012 → bloqueado solo en la tarea de "no firmar sin sincronizar". Se corrigieron ~25 referencias cruzadas en 006/007/008/009/010/011/012/013/014 que citaban a 005 para cosas que ahora viven en 015 (sucursalId/periodoEtiqueta/InspeccionDetalle/evidencia genérica/proxies de certificación) — quedaron repuntando a 015, con banners "⏸️ bloqueado" donde corresponde. **Lección**: al pausar o extraer una parte de un sprint ya referenciado por otros 8+ documentos, hay que rastrear cada mención de sus campos/archivos/entidades una por una — no basta con corregir el sprint origen.
- [x] **Gap encontrado en 013, corregido el mismo día a pedido del usuario**: `packages/ui/src/breadcrumb.tsx` importa `next/link` pero `packages/ui/package.json` nunca declaró `next` como dependencia (ni siquiera peer) — bloqueaba el type-checker de `next build`/`next lint` para todo `apps/web` (el bundler igual resolvía el módulo en runtime por hoisting de pnpm). **Corregido**: se agregó `"next": "^14.2.0"` como `peerDependency` y `devDependency` de `packages/ui` (mismo rango que `apps/web`), `pnpm install` enlazó `packages/ui/node_modules/next`. `next build` ahora compila y pasa el type-check sin error (verificado: "Compiled successfully" + "Linting and checking validity of types" en verde).
- [ ] **Gap distinto, sigue sin resolver**: `next build` falla más adelante en la generación estática de `/auth/login` (`useSearchParams() should be wrapped in a suspense boundary`) — preexistente desde la construcción inicial del login, sin relación con `packages/ui`. `next lint` tampoco puede correr en modo no interactivo todavía: nunca se eligió la configuración de ESLint (`Strict`/`Base`) la primera vez, y el comando pide esa elección interactivamente. Ambos quedan pendientes para `agente-frontend`/`agente-qa`.
- [ ] Primer módulo de dominio funcional más allá de auth (candidato: `fincas`, es la base de los demás).
- [ ] Pipeline de CI/CD en `.github/workflows/` (`agente-produccion`).
- [ ] Definir proveedor de hosting para `apps/api` (pendiente, ver `Claude.md` → Despliegue).
- [ ] Tests unitarios del módulo auth (`agente-qa`): casos de uso con mocks de puertos, componente/hook de login con Vitest + RTL.

---

## Cómo actualizar este archivo

- Mover ítems de "Pendiente" a "En progreso" cuando un agente empieza la tarea.
- Mover de "En progreso" a "Hecho" cuando termina y está verificado (no solo "escrito").
- Si una tarea se bloquea, anótalo en `errores_conocidos.md` con referencia desde aquí, no dupliques el detalle.
